// New OutputFormat implementation handling RedisValue to produce RESP for Raw format

impl OutputFormat {
    pub fn new_raw_format(redis_value: RedisValue) -> String {
        // logic to produce RESP format
    }
}